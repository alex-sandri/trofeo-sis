{
  "name": "trofeo-sis",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build.js && pipenv run static"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "bootstrap-italia": "^2.11.2"
  },
  "devDependencies": {
    "sass": "^1.81.0"
  }
}
